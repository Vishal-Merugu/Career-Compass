node_modules/
.DS_Store
.env
.idea/
.vscode/
*.log
.chrome-profile/
*cookies.json
dist/
.env.*
# ...but the template is meant to be committed: it documents which variables
# exist and holds only placeholders. It is already tracked, so `.env.*` never
# applied to it — this negation just makes the intent explicit, so a future
# `.env.something` template is not silently ignored.
!.env.example
# Probe output (contains egress IP)
probe-report.json
**/probe-report.json

# Build artefacts
extension.zip
dist-extension/
ext-prod/
# Dashboard build output: `client/` (vite) → here, served by express.static.
server/public/

# Session handoff written by /continue-session — per-session, not shared.
# tasks/lessons.md IS versioned; only the handoff is ignored.
tasks/handoff.md

# Résumé attachments uploaded through the dashboard. User data, and the
# deployed copy lives in a Docker volume rather than the repo.
server/uploads/
uploads/
